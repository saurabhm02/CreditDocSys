const User = require('./User.model');
const Document = require('./Document.model');
const CreditRequest = require('./CreditRequest.model');
const ScanHistory = require('./ScanHistory.model');

module.exports = {
  User,
  Document,
  CreditRequest,
  ScanHistory
}; 